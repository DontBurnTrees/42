# ex03 - ft_print_numbers

## Subject  
> Create a function that displays all digits on a single line, in ascending order.

## Objective  
> Learn how to iterate over characters using `while` loops.

## Going further   
> Why is the variable `numb` declared as `char` instead of `int` in this code?
> What would happen if you incremented beyond `'9'` without stopping the loop?  
