# ex00 - ft_putchar

## Subject  
> Write a function that displays the character passed as a parameter.

## Objective  
> Learn how to use the system function `write`.

## Going further  
> In the line `write(1, &c, 1);`, what does each argument represent?  
> Why is `1` used twice, and how would changing these values affect the behavior of the function?
